# Neo Music Backend — 기획/설계 노트

> Kotlin + Spring Boot(헥사고날) 백엔드. Flutter 앱 Neo Music의 서버.
> 원본 앱 계획서: ~/Downloads/PLAN.md 참고. 이 문서는 백엔드 관점의 기획·결정 기록.

## 제품 성격 (요약)
다국어 오디오 + **가사 위에 기타 코드**를 제공하는 큐레이션 뮤직/코드 플레이어.
곡은 서버가 배포, 앱은 다운로드 후 로컬 재생 + 델타 동기화.

---

## 확정된 기능 (사용자 답변 기준)

| 기능 | 여부 | 메모 |
|---|---|---|
| 사용자 로그인 | O | **닉네임 + 비번/PIN** 자체 회원가입 (개인정보 0, 이메일·전화 없음). 대상=어르신 → 소셜로그인 배제. BCrypt 해싱 + 로그인 시도 제한 필수. 복구 방식 미정 → Q3 |
| 좋아요 | X | 안 함 |
| 플레이리스트 | O | 사용자별 곡 모음 |
| 관리자 콘텐츠 업로드/관리 | O | 곡·오디오·가사·코드 등록/수정 |
| 검색 | O | 곡 검색 (범위 미정 → 추후) |
| 가사 + 기타 코드 | O | 가사 위에 코드 표시. **현재는 이미지**, 나중에 구조화 텍스트로 |
| 코드 트랜스포즈(코드 변경) | O | 사용자가 키를 올리고 내림. **카포 칸수**도 필요 |
| 가사 이미지 ↔ 텍스트 전환 | O | 사용자 편의에 따라 전환 (이미지/텍스트 둘 다 수용) |
| 통계 | △ | 있으면 좋음 (재생/조회 등, 우선순위 낮음) |

---

## 핵심 도메인 특이점 — 가사 & 코드
- 곡 = 가사(줄 단위) + 각 줄 위/특정 위치에 **기타 코드**(예: G, Cadd9, D/F#)
- **트랜스포즈**: 원곡 키 기준으로 반음 단위 이동 (예: +2 → G→A). 백엔드/클라 어디서 계산할지 결정 필요
- **카포(capo)**: N칸 잡으면 실제 소리나는 코드와 잡는 코드가 달라짐 → 표시 코드 변환
- 1차: 이미지로 제공 / 2차: 구조화 텍스트(줄+코드 위치+코드명)로 제공해 트랜스포즈·카포 동적 적용

---

## 미결정 (하나씩 질문으로 확정 예정)
- [x] Q2: 로그인 방식 → 닉네임+비번/PIN 자체 인증 (BCrypt, 로그인 제한). 개인정보 0
- [x] Q3: 비번 분실 대응 → **관리자 수동 초기화** (관리자 기능에 '비번 초기화' 포함). 본인확인은 운영으로 처리
- [x] Q4: 트랜스포즈/카포 → **트랜스포즈는 클라(오프라인)**. 1차는 이미지라 파싱·트랜스포즈 불필요(=2차 과제). 지금은 스키마만 확장 가능하게(chords.type=image/structured). 백엔드 트랜스포즈(코드 검증기+미리보기)는 2차 갈 때 결정 (실용 이유: 관리자 입력 검증)
- [x] Q5: 관리자 → **관리자 REST API + Swagger UI + 어드민 웹 화면**. 웹은 Thymeleaf로 백엔드 내장 제안(별도 프론트 X, 제주SK 경험 재활용). 곡·오디오·가사이미지·코드 등록/수정 + 비번 초기화
- [x] Q6: 검색 → **제목 검색(1차/MVP)** + **가사 검색(2차, 텍스트 가사 되면 전문검색)**. 아티스트 검색은 필요시 추가(trivial). 1차 이미지 가사는 검색 불가
- [x] Q7: 플레이리스트 → **여러 개 + 이름 + 곡 순서 지정 + 비공개(내 것만)**. 곡 추가/삭제/순서변경
- [x] Q8: 파일 저장 → **지금은 보류**. 단, 코드는 **StoragePort로 추상화**(로컬/S3 어댑터 교체, signed URL). 실제 저장소는 나중에 결정
- [x] 통계 → **Phase 2 nice-to-have** (재생/조회 집계). MVP 제외
- [ ] 델타 동기화(버전/rev/checksum/tombstone)는 카탈로그 API 설계에 반영 (계획서 §3.5)

---

## 확정 스코프 요약 (MVP)
- **인증**: 닉네임+비번/PIN 자체 회원가입·로그인 (BCrypt, 로그인 시도 제한, 개인정보 0). 관리자 비번 초기화
- **곡 카탈로그**: 곡/트랙(언어별 오디오)/가사(언어별, 1차 이미지) 모델 + 큐레이션 순서
- **델타 동기화**: `GET /catalog?since={version}` 변경/추가/삭제만 (rev/checksum/tombstone)
- **플레이리스트**: 여러 개 + 이름 + 순서, 비공개
- **검색**: 제목 (2차 가사 확장)
- **관리자**: REST API + Swagger + Thymeleaf 어드민 웹 (곡·파일·가사 CRUD, 비번 초기화)
- **파일**: StoragePort 추상화(로컬 어댑터로 시작), signed URL 설계

## 확장 (Phase 2+)
- 구조화 텍스트 가사 + 기타 코드 + 트랜스포즈/카포(클라 계산, 백엔드 검증기 검토)
- 가사 이미지 ↔ 텍스트 사용자 전환 / 통계 / 백그라운드 재생(앱)

## 기술 스택 (백엔드)
Kotlin · Spring Boot 3 · JPA(+필요시 QueryDSL) · 헥사고날+DDD · MySQL(운영)/H2(개발·테스트)
· Redis(캐싱, 여유 시) · JWT · springdoc(Swagger) · Thymeleaf(어드민) · JUnit/Kotest · GitHub Actions · Docker

## 다음 단계 (천천히, 하나씩)
1. [x] **데이터 모델 설계** → docs/DATA_MODEL.md (확정: 저장키+signed URL, rev 제거, Song id 문자열)
2. [x] **프로젝트 뼈대** → Gradle(Kotlin+SpringBoot3.3.5+JPA+H2) 빌드 성공, contextLoads 테스트 통과. base package `com.hyobin.neomusic`
3. [x] **카탈로그 도메인 모델** → 값객체(SongId/Lang/StorageKey/Checksum)+Track/Lyric/Song(애그리거트). 순수 Kotlin, 단위 테스트 8개 통과
4. [x] **영속성 어댑터** → 출력포트(Save/Load) + JPA 엔티티(Song/Track/Lyric) + 매퍼 + 어댑터. 왕복·UPSERT 통합테스트 통과
5. [x] **카탈로그 API + 델타 동기화** → CatalogVersion + CatalogService(register/delete/getCatalog) + GET /catalog?since=. 델타/tombstone/웹 테스트 통과 (총 15개 green)
6. 인증 (회원가입/로그인/JWT/관리자 초기화)
7. 관리자 곡 등록/삭제 API + 어드민 웹
8. 플레이리스트 → 검색
9. 파일 StoragePort(로컬) + signed URL
10. CI + README + git
